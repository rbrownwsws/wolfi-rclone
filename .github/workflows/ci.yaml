# $schema: https://json.schemastore.org/github-workflow.json
name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    uses: rbrownwsws/melange-workflows/.github/workflows/build.yaml@a94551e03d10ebfa707c6f694a47e9d62164b493 # master
    with:
      build-arch: "amd64,arm64"
