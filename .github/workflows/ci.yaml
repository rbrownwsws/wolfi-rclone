# $schema: https://json.schemastore.org/github-workflow.json
name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    uses: rbrownwsws/melange-workflows/.github/workflows/build.yaml@5834f8f85f7e8afd64386124bb879ee374bfef0d # master
    with:
      build-arch: "amd64,arm64"
