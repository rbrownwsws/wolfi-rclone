# $schema: https://json.schemastore.org/github-workflow.json
name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    uses: rbrownwsws/melange-workflows/.github/workflows/build.yaml@ea177c7f7882e5f8b547a0910ad0337c5c0bb73e # master
    with:
      build-arch: "amd64,arm64"
