# $schema: https://json.schemastore.org/github-workflow.json
name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    uses: rbrownwsws/melange-workflows/.github/workflows/build.yaml@5ea10d05285d312b8d540c7d7a3aaa3cc0502bd7 # master
    with:
      build-arch: "amd64,arm64"
