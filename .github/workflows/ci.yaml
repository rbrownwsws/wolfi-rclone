# $schema: https://json.schemastore.org/github-workflow.json
name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    uses: rbrownwsws/melange-workflows/.github/workflows/build.yaml@e9dd2f3106c028e79aeed93ed4d66b22f5558062 # master
    with:
      build-arch: "amd64,arm64"
