# $schema: https://json.schemastore.org/github-workflow.json
name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    uses: rbrownwsws/melange-workflows/.github/workflows/build.yaml@a32dcea09db99f01e2225d29129464657e93ee58 # v1.0.2
    with:
      build-arch: "amd64,arm64"
