# $schema: https://json.schemastore.org/github-workflow.json
name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    uses: rbrownwsws/melange-workflows/.github/workflows/build.yaml@1e3926e12fe67275f9303fc16b7a25a2a1117d82 # master
    with:
      build-arch: "amd64,arm64"
