# $schema: https://json.schemastore.org/github-workflow.json
name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    uses: rbrownwsws/melange-workflows/.github/workflows/build.yaml@9c5d28b36d35087982dd30f019fd9b2339cceca1 # master
    with:
      build-arch: "amd64,arm64"
