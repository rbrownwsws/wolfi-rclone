# $schema: https://json.schemastore.org/github-workflow.json
name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    uses: rbrownwsws/melange-workflows/.github/workflows/build.yaml@41b2004dc8734a07a80c6c21c533b1626a8c7c5b # master
    with:
      build-arch: "amd64,arm64"
