# $schema: https://json.schemastore.org/github-workflow.json
name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    uses: rbrownwsws/melange-workflows/.github/workflows/build.yaml@6f9ec55d7097d1ec59c9fe4f866ebfa08dc7bcf7 # master
    with:
      build-arch: "amd64,arm64"
