# $schema: https://json.schemastore.org/github-workflow.json
name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    uses: rbrownwsws/melange-workflows/.github/workflows/build.yaml@307802d5a8603ae7d624ab6b5c5ecb3b91c82a4c # master
    with:
      build-arch: "amd64,arm64"
