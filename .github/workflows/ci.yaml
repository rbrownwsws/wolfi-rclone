# $schema: https://json.schemastore.org/github-workflow.json
name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    uses: rbrownwsws/melange-workflows/.github/workflows/build.yaml@dc49ddcbbcf634a1417d72229c651677d278d07d # master
    with:
      build-arch: "amd64,arm64"
