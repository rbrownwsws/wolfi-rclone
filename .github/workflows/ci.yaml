# $schema: https://json.schemastore.org/github-workflow.json
name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    uses: rbrownwsws/melange-workflows/.github/workflows/build.yaml@8442aa3ca770f152de8a223659ea3f054f70c61b # master
    with:
      build-arch: "amd64,arm64"
