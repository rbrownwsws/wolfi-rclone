# $schema: https://json.schemastore.org/github-workflow.json
name: Check - Build

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    uses: rbrownwsws/melange-workflows/.github/workflows/build.yaml@0029c936d442a310e6f3c27317d5d54996989465 # v1.0.5
    with:
      build-arch: "amd64,arm64"
