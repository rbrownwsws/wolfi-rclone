# $schema: https://json.schemastore.org/github-workflow.json
name: Check - Build

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    uses: rbrownwsws/melange-workflows/.github/workflows/build.yaml@f2d8c2ebfb2f52a77949553045d7eef622000822 # v1.0.3
    with:
      build-arch: "amd64,arm64"
