# $schema: https://json.schemastore.org/github-workflow.json
name: Check - Build

on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    uses: rbrownwsws/melange-workflows/.github/workflows/build.yaml@a6a682adad01e1f869590e9ec3651cdf9ef4df52 # v1.0.7
    with:
      build-arch: "amd64,arm64"
