# $schema: https://json.schemastore.org/github-workflow.json
name: Update Package Repo

on:
  workflow_dispatch:
  release:
    types:
      - released

permissions: {}

jobs:
  update-package-repo:
    permissions:
      contents: read   # to get packages from Releases
      pages: write     # to deploy to Pages
      id-token: write  # to verify the deployment originates from an appropriate source

    uses: rbrownwsws/melange-workflows/.github/workflows/update-package-repo.yaml@0029c936d442a310e6f3c27317d5d54996989465 # v1.0.5
    with:
      signing-key-name: ${{ vars.APK_SIGNING_KEY_NAME }}
    secrets:
      signing-key: ${{ secrets.APK_SIGNING_KEY }}
