# $schema: https://json.schemastore.org/github-workflow.json
name: Release

on:
  workflow_dispatch:

jobs:
  release:
    uses: rbrownwsws/melange-workflows/.github/workflows/release.yaml@1bbf2e5581f51e3ac6887acfc39197c91e4f056a # master
    with:
      build-arch: "amd64,arm64"
      signing-key-name: ${{ vars.APK_SIGNING_KEY_NAME }}
      publish-release: false
      releaser-app-client-id: ${{ vars.RELEASER_APP_CLIENT_ID }}
    secrets:
      signing-key: ${{ secrets.APK_SIGNING_KEY }}
      releaser-app-private-key: ${{ secrets.RELEASER_APP_PRIVATE_KEY }}
    permissions:
      id-token: write
      attestations: write
      artifact-metadata: write
